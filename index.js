const body = document.querySelector('body')
const viewHTMLList = document.querySelectorAll('.viewHTML')
const viewCSSList = document.querySelectorAll('.viewCSS')

body.addEventListener('click', (e) => {
  const dom = e.target
})